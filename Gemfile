source "https://rubygems.org"

# Pins your local build to exactly what GitHub Pages runs — matching gem versions.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.0+ dropped webrick from stdlib; `jekyll serve` needs it locally.
gem "webrick"
